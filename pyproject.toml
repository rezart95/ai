[project]
name = "ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.0.2",
    "langgraph>=1.0.1",
    "pre-commit>=4.3.0",
]

[tool.ruff]
lint.ignore = ["F401", "E402", "F841"]
